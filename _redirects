# Redirect www to non-www
https://www.mpermon.com/* https://mpermon.com/:splat 301!

# Redirect HTTP to HTTPS (handled by Cloudflare, but explicit is good)
http://mpermon.com/* https://mpermon.com/:splat 301!
http://www.mpermon.com/* https://mpermon.com/:splat 301!

# Ensure trailing slashes are handled consistently
# Redirect index.html to root
/index.html / 301

# Handle 404s - redirect to homepage
/* /index.html 404
